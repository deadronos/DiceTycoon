import { describe, it, expect } from 'vitest';

// This test file is disabled because Controls component doesn't exist
// The app now uses RollButton and AutorollControls components
describe.skip('Controls (legacy)', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
