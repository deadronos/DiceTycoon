import { AppContainer } from './AppContainer';
import './styles.css';

/**
 * The root component of the Dice Tycoon application.
 * Now acts as a thin wrapper/entry point for the AppContainer.
 */
export const App = AppContainer;

export default App;
